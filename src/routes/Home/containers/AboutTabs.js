import { connect } from 'react-redux';

import { AboutTabs } from '../components/AboutTabs';


const mapStateToProps = (/* state */) => ({
});

const mapDispatchToProps = {
};

export default connect(mapStateToProps, mapDispatchToProps)(AboutTabs);
