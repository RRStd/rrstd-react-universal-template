import { connect } from 'react-redux';

import { NewsPageToolbar } from '../components/NewsPageToolbar';


const mapStateToProps = (/* state */) => ({
});

const mapDispatchToProps = {
};

export default connect(mapStateToProps, mapDispatchToProps)(NewsPageToolbar);
